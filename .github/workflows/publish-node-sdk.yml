name: Publish Node SDK

on:
  push:
    tags:
      - 'node-sdk-v*'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --filter @sharkauth/node

      - name: Build
        run: pnpm --filter @sharkauth/node build

      - name: Publish to npm
        run: pnpm --filter @sharkauth/node publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Verify post-publish availability
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#node-sdk-v}"
          echo "Verifying @sharkauth/node@${VERSION} on npm…"
          for i in 1 2 3; do
            if npm view "@sharkauth/node@${VERSION}" version 2>/dev/null | grep -q "${VERSION}"; then
              echo "@sharkauth/node@${VERSION} available on npm."
              exit 0
            fi
            echo "Attempt ${i}/3 failed — sleeping 30s…"
            sleep 30
          done
          echo "ERROR: @sharkauth/node@${VERSION} not yet visible on npm after 3 attempts."
          exit 1
