# user_show.go

**Path:** `cmd/shark/cmd/user_show.go`
**Package:** `cmd`

## Purpose
Implements `shark user show <id>` — fetches a single user via GET /api/v1/users/{id}.

## Key types / functions
- `userShowCmd` — cobra command, `--json` flag supported.
