# user_delete.go

**Path:** `cmd/shark/cmd/user_delete.go`
**Package:** `cmd`

## Purpose
Implements `shark user delete <id>` — deletes a user via DELETE /api/v1/users/{id}. Requires confirmation unless `--yes`.
