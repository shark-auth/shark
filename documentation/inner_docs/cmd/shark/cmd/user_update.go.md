# user_update.go

**Path:** `cmd/shark/cmd/user_update.go`
**Package:** `cmd`

## Purpose
Implements `shark user update <id>` — patches a user via PATCH /api/v1/users/{id}. Supports `--name`, `--email`, `--email-verified`.
