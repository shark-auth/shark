// Compat shim — Settings was rebuilt as a unified config dashboard.
// SystemSettings is now an alias for Settings to keep external imports working.
export { Settings as SystemSettings } from './settings'
