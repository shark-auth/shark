import React from 'react'
import type { HostedConfig } from '../../hosted-entry'

interface SignupPageProps {
  config: HostedConfig
}

export function SignupPage(_props: SignupPageProps) {
  return <div>signup stub</div>
}
